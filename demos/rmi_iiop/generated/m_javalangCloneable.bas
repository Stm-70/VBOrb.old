Attribute VB_Name = "m_javalangCloneable"
'Generated by IDL2VB v121. Copyright (c) 2000-2003 Martin.Both
'Source File Name: jdk1.4/idl/java/lang/Cloneable.idl

Option Explicit

'interface abstract ::java::lang::Cloneable
Public Const TypeId As String = "RMI:java.lang.Cloneable:0000000000000000"

'Helper
Public Function narrow(ByVal Abstr As cOrbAbstractBase) As c_javalangCloneable
    On Error GoTo ErrHandler
    If Abstr Is Nothing Then
        Set narrow = Nothing
    ElseIf Abstr.isObjRef() Then
        Dim Obj As cOrbObject
        Set Obj = Abstr
        Dim oObj As cOrbObject
        Set oObj = New c_javalangCloneable
        If oObj.setObjRef(Obj.getObjRef(), True) Then
            Set narrow = Nothing
        Else
            Set narrow = oObj
        End If
    Else
        Set narrow = Abstr
    End If
    Exit Function
ErrHandler:
    Call VBOrb.ErrReraise(Err, "narrow")
End Function

'Helper
Public Function uncheckedNarrow(ByVal Abstr As cOrbAbstractBase) _
    As c_javalangCloneable
    On Error GoTo ErrHandler
    If Abstr Is Nothing Then
        Set uncheckedNarrow = Nothing
    ElseIf Abstr.isObjRef() Then
        Dim Obj As cOrbObject
        Set Obj = Abstr
        Dim oObj As cOrbObject
        Set oObj = New c_javalangCloneable
        If oObj.setObjRef(Obj.getObjRef(), False) Then
            Set uncheckedNarrow = Nothing
        Else
            Set uncheckedNarrow = oObj
        End If
    Else
        Set uncheckedNarrow = Abstr
    End If
    Exit Function
ErrHandler:
    Call VBOrb.ErrReraise(Err, "uncheckedNarrow")
End Function
